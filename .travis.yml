language: node_js
sudo: false
node_js:
- 'node'

addons:
  chrome: stable

cache:
  yarn: true
  directories:
    - node_modules
    - "$HOME/.npm"
    - "$HOME/.cache"

branches:
  only:
    - master

install:
  - yarn install

jobs:
  include:
    - stage: test
      script:
        - yarn install
        - echo Hello World
    - stage: deploy
      script:
        - yarn functions:install
        - yarn build
        - yarn firebase deploy --token $FIREBASE_TOKEN --non-interactive

stages:
  - test
  - name: deploy
    if: test $TRAVIS_BRANCH = "master" && test $TRAVIS_PULL_REQUEST = "false"
