import { IconButtonExample01Module } from './icon-button-example-01.module';

describe('IconButtonExample01Module', () => {
  let iconButtonExample01Module: IconButtonExample01Module;

  beforeEach(() => {
    iconButtonExample01Module = new IconButtonExample01Module();
  });

  it('should create an instance', () => {
    expect(iconButtonExample01Module).toBeTruthy();
  });
});
