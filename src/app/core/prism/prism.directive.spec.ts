import { PrismDirective } from './prism.directive';

describe('PrismDirective', () => {
  it('should create an instance', () => {
    const directive = new PrismDirective();
    expect(directive).toBeTruthy();
  });
});
