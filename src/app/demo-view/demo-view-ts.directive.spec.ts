import { DemoViewTsDirective } from './demo-view-ts.directive';

describe('DemoViewTsDirective', () => {
  it('should create an instance', () => {
    const directive = new DemoViewTsDirective();
    expect(directive).toBeTruthy();
  });
});
