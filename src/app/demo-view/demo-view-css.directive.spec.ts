import { DemoViewCssDirective } from './demo-view-css.directive';

describe('DemoViewCssDirective', () => {
  it('should create an instance', () => {
    const directive = new DemoViewCssDirective();
    expect(directive).toBeTruthy();
  });
});
