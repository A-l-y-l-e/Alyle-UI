import { DemoViewHtmlDirective } from './demo-view-html.directive';

describe('DemoViewHtmlDirective', () => {
  it('should create an instance', () => {
    const directive = new DemoViewHtmlDirective();
    expect(directive).toBeTruthy();
  });
});
