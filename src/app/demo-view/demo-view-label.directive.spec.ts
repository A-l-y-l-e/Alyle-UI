import { DemoViewLabelDirective } from './demo-view-label.directive';

describe('DemoViewLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new DemoViewLabelDirective();
    expect(directive).toBeTruthy();
  });
});
