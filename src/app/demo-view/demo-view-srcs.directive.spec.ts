import { DemoViewSrcsDirective } from './demo-view-srcs.directive';

describe('DemoViewSrcsDirective', () => {
  it('should create an instance', () => {
    const directive = new DemoViewSrcsDirective();
    expect(directive).toBeTruthy();
  });
});
