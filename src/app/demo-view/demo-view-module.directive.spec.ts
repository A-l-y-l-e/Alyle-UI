import { DemoViewModuleDirective } from './demo-view-module.directive';

describe('DemoViewModuleDirective', () => {
  it('should create an instance', () => {
    const directive = new DemoViewModuleDirective();
    expect(directive).toBeTruthy();
  });
});
