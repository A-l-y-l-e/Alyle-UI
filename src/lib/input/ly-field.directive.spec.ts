import { LyFieldDirective } from './ly-field.directive';

describe('LyFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new LyFieldDirective();
    expect(directive).toBeTruthy();
  });
});
