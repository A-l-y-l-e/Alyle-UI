import { LyCard } from './card.directive';

describe('CardDirective', () => {
  it('should create an instance', () => {
    const directive = new LyCard();
    expect(directive).toBeTruthy();
  });
});
