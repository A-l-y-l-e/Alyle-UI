import { MediaDirective } from './media.directive';

describe('MediaDirective', () => {
  it('should create an instance', () => {
    const directive = new MediaDirective();
    expect(directive).toBeTruthy();
  });
});
