import { LyRippleDirective } from './ripple.directive';

describe('LyRippleDirective', () => {
  it('should create an instance', () => {
    const directive = new LyRippleDirective();
    expect(directive).toBeTruthy();
  });
});
